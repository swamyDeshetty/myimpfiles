1)iterators=>
iterators are used to iterate over the elements,collections like list,tuple..
iterators are used to implement the iter and next method to iterate the elements.
we generally use the loops to iterate over the collections like list,tuple..
 Ex: a=[1,2,4,5]
x=iter(a)
print(next(x))
print(next(x))
print(next(x))